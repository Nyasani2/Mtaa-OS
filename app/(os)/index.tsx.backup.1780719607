// app/(os)/index.tsx — MTAA OS Home v2: Layered Ecosystem Dashboard
// All MTAA core apps pre-installed. AppStore = third-party apps + updates only.

import React, { useEffect, useState, useCallback } from "react";
import {
  View,
  Text,
  TouchableOpacity,
  StyleSheet,
  Image,
  Dimensions,
  ScrollView,
  Platform,
  Animated,
} from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { useRouter } from "expo-router";
import { useIdentity } from "@/lib/auth/use-identity";
import { Ionicons, MaterialCommunityIcons, FontAwesome5 } from "@expo/vector-icons";

const { width, height } = Dimensions.get("window");
const COLS = 4;
const TILE = (width - 48) / COLS;

// ─── Layer Definitions ───
interface AppTile {
  id: string;
  name: string;
  icon: string;
  iconSet: "ionicons" | "material" | "fontawesome";
  route: string;
  description: string;
  color: string;
  badge?: string;
}

interface LayerSection {
  id: string;
  title: string;
  subtitle: string;
  color: string;
  apps: AppTile[];
}

const LAYERS: LayerSection[] = [
  {
    id: "social",
    title: "SOCIAL LAYER",
    subtitle: "People Connect Here",
    color: "#0EA5E9",
    apps: [
      { id: "messages", name: "Messages", icon: "chatbubbles", iconSet: "ionicons", route: "/messages", description: "Messaging, channels, and communication infrastructure.", color: "#0EA5E9" },
      { id: "tribes", name: "Tribes", icon: "people", iconSet: "ionicons", route: "/tribes", description: "Communities, events, and identity groups.", color: "#EC4899" },
      { id: "feed", name: "Feed", icon: "newspaper", iconSet: "ionicons", route: "/feed", description: "Real-time social and civic discovery stream.", color: "#8B5CF6" },
      { id: "gallery", name: "Gallery", icon: "images", iconSet: "ionicons", route: "/gallery", description: "Media, memories, and content library.", color: "#A855F7" },
      { id: "camera", name: "Camera", icon: "camera", iconSet: "ionicons", route: "/camera", description: "Capture photos and videos.", color: "#EF4444" },
      { id: "phone", name: "Phone", icon: "call", iconSet: "ionicons", route: "/phone", description: "Voice and video communication layer.", color: "#10B981" },
    ],
  },
  {
    id: "economic",
    title: "ECONOMIC LAYER",
    subtitle: "People Work & Earn Here",
    color: "#10B981",
    apps: [
      { id: "wallet", name: "Wallet", icon: "wallet", iconSet: "ionicons", route: "/wallet", description: "Payments, transfers, escrow, and treasury rails.", color: "#10B981", badge: "Live" },
      { id: "jobs", name: "Jobs", icon: "briefcase", iconSet: "ionicons", route: "/jobs", description: "Africa-wide workforce, hiring, payroll, and opportunity platform.", color: "#059669" },
      { id: "marketplace", name: "Marketplace", icon: "cart", iconSet: "ionicons", route: "/marketplace", description: "Commerce layer for goods, services, and trade.", color: "#D97706" },
      { id: "shop", name: "Shop", icon: "storefront", iconSet: "ionicons", route: "/shop", description: "Retail and merchant storefronts.", color: "#DC2626" },
      { id: "mtaxi", name: "MTaxi", icon: "car", iconSet: "ionicons", route: "/mtaxi", description: "Ride-hailing and mobility coordination.", color: "#F59E0B" },
      { id: "mtruck", name: "MTruck", icon: "truck", iconSet: "ionicons", route: "/mtruck", description: "Freight, logistics, and delivery systems.", color: "#7C3AED" },
    ],
  },
  {
    id: "civic",
    title: "CIVIC LAYER",
    subtitle: "Governments Operate Here",
    color: "#F59E0B",
    apps: [
      { id: "command", name: "Command Centre", icon: "business", iconSet: "ionicons", route: "/civic/command", description: "National operating dashboard and infrastructure intelligence.", color: "#1E40AF" },
      { id: "treasury", name: "Treasury", icon: "cash", iconSet: "ionicons", route: "/civic/treasury", description: "Public finance, escrow, expenditure, and budgeting.", color: "#059669" },
      { id: "revenue", name: "Revenue Authority", icon: "receipt", iconSet: "ionicons", route: "/civic/revenue", description: "Taxes, levies, billing, and collections.", color: "#047857" },
      { id: "police", name: "Police", icon: "shield", iconSet: "ionicons", route: "/civic/police", description: "Public safety, dispatch, and emergency coordination.", color: "#7C2D12" },
      { id: "courts", name: "Courts", icon: "scale", iconSet: "ionicons", route: "/civic/courts", description: "Justice systems, case management, and legal infrastructure.", color: "#581C87" },
      { id: "health", name: "Health", icon: "medical", iconSet: "ionicons", route: "/health", description: "Healthcare access, records, and emergency response.", color: "#0D9488" },
      { id: "education", name: "Education", icon: "school", iconSet: "ionicons", route: "/education", description: "Learning, certification, and knowledge infrastructure.", color: "#EA580C" },
      { id: "streets", name: "Streets", icon: "map", iconSet: "ionicons", route: "/streets", description: "Urban infrastructure, roads, and public works.", color: "#4338CA" },
    ],
  },
];

// ─── Intelligence Simulation Data ───
const LIVE_ACTIVITIES = [
  { icon: "💰", text: "Revenue collected in Nairobi County" },
  { icon: "🚢", text: "Cargo cleared at Mombasa Port" },
  { icon: "🏥", text: "Hospital bed allocated in Kisumu" },
  { icon: "🚨", text: "Security alert resolved in Eldoret" },
  { icon: "💼", text: "Job matched in Lagos network" },
  { icon: "🚗", text: "Driver allocation active" },
  { icon: "📍", text: "1,240 vehicles online" },
  { icon: "📊", text: "Demand spike detected" },
  { icon: "⚡", text: "Optimizing routes" },
];

const GOVERNANCE_ALERTS = [
  { type: "risk", icon: "⚠️", text: "Low corruption anomaly detected in 3 counties" },
  { type: "economy", icon: "📈", text: "Job matching efficiency increased 12%" },
  { type: "alert", icon: "🔴", text: "Infrastructure delay detected in transport layer" },
  { type: "stable", icon: "🏛️", text: "Economy stable — 2.4% growth trend detected" },
  { type: "alert", icon: "🚗", text: "Transport congestion rising in Nairobi CBD" },
  { type: "recommend", icon: "💡", text: "Recommendation: redistribute civic maintenance budget" },
];

const NATIONAL_STREAM = [
  { icon: "💰", text: "Nairobi County revenue updated" },
  { icon: "🚨", text: "Police response dispatched in Mombasa" },
  { icon: "🏥", text: "Hospital capacity adjusted in Kisumu" },
  { icon: "🚧", text: "Road maintenance flagged in Eldoret" },
  { icon: "📊", text: "National tax flow updated" },
  { icon: "⚡", text: "Electricity grid balancing active" },
];

// ─── Render Icon Helper ───
function RenderIcon({ app, size = 28 }: { app: AppTile; size?: number }) {
  const color = "#FFFFFF";
  if (app.iconSet === "material") {
    return <MaterialCommunityIcons name={app.icon as any} size={size} color={color} />;
  }
  if (app.iconSet === "fontawesome") {
    return <FontAwesome5 name={app.icon as any} size={size} color={color} />;
  }
  return <Ionicons name={app.icon as any} size={size} color={color} />;
}

// ─── Animated Pulse Dot ───
function PulseDot({ color }: { color: string }) {
  const pulse = new Animated.Value(1);
  useEffect(() => {
    const anim = Animated.loop(
      Animated.sequence([
        Animated.timing(pulse, { toValue: 1.5, duration: 800, useNativeDriver: true }),
        Animated.timing(pulse, { toValue: 1, duration: 800, useNativeDriver: true }),
      ])
    );
    anim.start();
    return () => anim.stop();
  }, []);
  return (
    <Animated.View
      style={[
        styles.pulseDot,
        { backgroundColor: color, transform: [{ scale: pulse }] },
      ]}
    />
  );
}

// ─── Main Component ───
export default function OSHomeScreen() {
  const router = useRouter();
  const { user } = useIdentity();
  const [currentDate, setCurrentDate] = useState(new Date());
  const [selectedView, setSelectedView] = useState<"citizen" | "governor" | "president" | "investor">("citizen");

  useEffect(() => {
    const timer = setInterval(() => setCurrentDate(new Date()), 60000);
    return () => clearInterval(timer);
  }, []);

  const getGreeting = () => {
    const hour = currentDate.getHours();
    if (hour < 12) return "Good morning";
    if (hour < 17) return "Good afternoon";
    return "Good evening";
  };

  const days = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
  const months = [
    "January", "February", "March", "April", "May", "June",
    "July", "August", "September", "October", "November", "December"
  ];

  const launchApp = useCallback((route: string) => {
    router.push(route as any);
  }, [router]);

  const renderAppTile = (app: AppTile) => (
    <TouchableOpacity
      key={app.id}
      style={styles.appTile}
      onPress={() => launchApp(app.route)}
      activeOpacity={0.7}
    >
      <View style={[styles.iconContainer, { backgroundColor: app.color }]}>
        <RenderIcon app={app} size={26} />
        {app.badge && (
          <View style={styles.badge}>
            <Text style={styles.badgeText}>{app.badge}</Text>
          </View>
        )}
      </View>
      <Text style={styles.appName} numberOfLines={1}>{app.name}</Text>
      <Text style={styles.appDesc} numberOfLines={2}>{app.description}</Text>
    </TouchableOpacity>
  );

  const renderLayerSection = (layer: LayerSection) => (
    <View key={layer.id} style={styles.layerSection}>
      <View style={styles.layerHeader}>
        <Text style={[styles.layerTitle, { color: layer.color }]}>{layer.title}</Text>
        <Text style={styles.layerSubtitle}>{layer.subtitle}</Text>
        <Text style={styles.layerDesc}>
          {layer.id === "social" && "Communication, culture, communities, creators, and real-time interaction."}
          {layer.id === "economic" && "Jobs, payments, mobility, logistics, trade, and commerce infrastructure."}
          {layer.id === "civic" && "Treasury, revenue, police, courts, ports, infrastructure, national systems."}
        </Text>
      </View>
      <View style={styles.appsGrid}>
        {layer.apps.map(renderAppTile)}
      </View>
    </View>
  );

  const renderIntelligenceLayer = () => (
    <View style={styles.layerSection}>
      <View style={styles.layerHeader}>
        <Text style={[styles.layerTitle, { color: "#6366F1" }]}>INTELLIGENCE LAYER</Text>
        <Text style={styles.layerSubtitle}>System Intelligence</Text>
        <Text style={styles.layerDesc}>
          Real-time analytics, governance AI, and continental infrastructure monitoring.
        </Text>
      </View>

      {/* View Switcher */}
      <View style={styles.viewSwitcher}>
        {(["citizen", "governor", "president", "investor"] as const).map((view) => (
          <TouchableOpacity
            key={view}
            style={[
              styles.viewButton,
              selectedView === view && styles.viewButtonActive,
            ]}
            onPress={() => setSelectedView(view)}
          >
            <Text
              style={[
                styles.viewButtonText,
                selectedView === view && styles.viewButtonTextActive,
              ]}
            >
              {view.charAt(0).toUpperCase() + view.slice(1)} View
            </Text>
          </TouchableOpacity>
        ))}
      </View>

      {/* Africa Map Placeholder */}
      <View style={styles.mapCard}>
        <View style={styles.mapHeader}>
          <Text style={styles.mapTitle}>Nation Intelligence Map</Text>
          <View style={styles.mapDropdown}>
            <Text style={styles.mapDropdownText}>Africa View</Text>
            <Ionicons name="chevron-down" size={14} color="#94A3B8" />
          </View>
        </View>
        <View style={styles.mapBody}>
          <View style={styles.mapDots}>
            <View style={[styles.mapDot, { top: "30%", left: "45%", backgroundColor: "#0EA5E9" }]} />
            <View style={[styles.mapDot, { top: "50%", left: "35%", backgroundColor: "#F59E0B" }]} />
            <View style={[styles.mapDot, { top: "40%", left: "55%", backgroundColor: "#10B981" }]} />
            <View style={[styles.mapDot, { top: "60%", left: "50%", backgroundColor: "#EC4899" }]} />
            <View style={[styles.mapDot, { top: "25%", left: "40%", backgroundColor: "#8B5CF6" }]} />
          </View>
          <Text style={styles.mapLabel}>Africa Macro Economic + Civic Grid</Text>
        </View>
      </View>

      {/* Live Activity + National Stream */}
      <View style={styles.intelRow}>
        <View style={[styles.intelCard, { flex: 1, marginRight: 8 }]}>
          <View style={styles.intelCardHeader}>
            <PulseDot color="#10B981" />
            <Text style={styles.intelCardTitle}>Live System Activity</Text>
            <Text style={styles.simLabel}>(Simulation)</Text>
          </View>
          {LIVE_ACTIVITIES.slice(0, 5).map((act, i) => (
            <View key={i} style={styles.activityRow}>
              <Text style={styles.activityIcon}>{act.icon}</Text>
              <Text style={styles.activityText}>{act.text}</Text>
            </View>
          ))}
        </View>

        <View style={[styles.intelCard, { flex: 1, marginLeft: 8 }]}>
          <View style={styles.intelCardHeader}>
            <Ionicons name="globe" size={14} color="#0EA5E9" />
            <Text style={styles.intelCardTitle}>National Activity Stream</Text>
          </View>
          {NATIONAL_STREAM.map((item, i) => (
            <View key={i} style={styles.activityRow}>
              <Text style={styles.activityIcon}>{item.icon}</Text>
              <Text style={styles.activityText}>{item.text}</Text>
            </View>
          ))}
        </View>
      </View>

      {/* Governance AI */}
      <View style={styles.governanceCard}>
        <View style={styles.governanceHeader}>
          <Ionicons name="brain" size={18} color="#6366F1" />
          <Text style={styles.governanceTitle}>MTAA Governance AI</Text>
        </View>
        <Text style={styles.governanceSub}>
          System intelligence layer analyzing national operations in real-time.
        </Text>
        {GOVERNANCE_ALERTS.map((alert, i) => (
          <View key={i} style={styles.alertRow}>
            <Text style={styles.alertIcon}>{alert.icon}</Text>
            <Text
              style={[
                styles.alertText,
                alert.type === "risk" && { color: "#F87171" },
                alert.type === "economy" && { color: "#34D399" },
                alert.type === "alert" && { color: "#FBBF24" },
                alert.type === "stable" && { color: "#60A5FA" },
                alert.type === "recommend" && { color: "#A78BFA" },
              ]}
            >
              {alert.type === "risk" && "Risk: "}
              {alert.type === "economy" && "Economy: "}
              {alert.type === "alert" && "Alert: "}
              {alert.type === "stable" && "Economy stable — "}
              {alert.type === "recommend" && "Recommendation: "}
              {alert.text.replace(/^(Risk|Economy|Alert|Recommendation): /, "")}
            </Text>
          </View>
        ))}
      </View>

      {/* MTaxi System Status */}
      <View style={styles.systemCard}>
        <View style={styles.systemHeader}>
          <View style={styles.systemStatus}>
            <View style={[styles.statusDot, { backgroundColor: "#10B981" }]} />
            <Text style={styles.systemTitle}>MTaxi — National Mobility System</Text>
          </View>
        </View>
        <View style={styles.systemMetrics}>
          <View style={styles.metric}>
            <Ionicons name="car" size={14} color="#F59E0B" />
            <Text style={styles.metricText}>Driver allocation active</Text>
          </View>
          <View style={styles.metric}>
            <Ionicons name="location" size={14} color="#EF4444" />
            <Text style={styles.metricText}>1,240 vehicles online</Text>
          </View>
          <View style={styles.metric}>
            <Ionicons name="trending-up" size={14} color="#3B82F6" />
            <Text style={styles.metricText}>Demand spike detected</Text>
          </View>
          <View style={styles.metric}>
            <Ionicons name="flash" size={14} color="#F59E0B" />
            <Text style={styles.metricText}>Optimizing routes</Text>
          </View>
        </View>
      </View>
    </View>
  );

  return (
    <SafeAreaView style={styles.container}>
      {/* Background Image */}
      <Image
        source={{ uri: "https://images.unsplash.com/photo-1547471080-7cc2caa01a7e?w=800&q=80" }}
        style={styles.backgroundImage}
        resizeMode="cover"
      />
      <View style={styles.overlay} />

      {/* Header */}
      <View style={styles.header}>
        <View>
          <Text style={styles.greeting}>{getGreeting()},</Text>
          <Text style={styles.userName}>
            {user?.email?.split("@")[0] || "User"}
          </Text>
        </View>
        <View style={styles.headerActions}>
          <TouchableOpacity
            style={styles.iconButton}
            onPress={() => router.push("/notifications" as any)}
          >
            <Ionicons name="notifications" size={22} color="#FFFFFF" />
            <View style={styles.notifBadge} />
          </TouchableOpacity>
          <TouchableOpacity
            style={styles.iconButton}
            onPress={() => router.push("/settings" as any)}
          >
            <Ionicons name="settings" size={22} color="#FFFFFF" />
          </TouchableOpacity>
        </View>
      </View>

      {/* Date Display */}
      <View style={styles.dateContainer}>
        <Text style={styles.dayName}>{days[currentDate.getDay()].toUpperCase()}</Text>
        <Text style={styles.dayNum}>{currentDate.getDate()}</Text>
        <Text style={styles.monthYear}>
          {months[currentDate.getMonth()]} {currentDate.getFullYear()}
        </Text>
      </View>

      <ScrollView
        style={styles.content}
        showsVerticalScrollIndicator={false}
        contentContainerStyle={styles.scrollContent}
      >
        {/* AppStore Banner */}
        <TouchableOpacity
          style={styles.appStoreBanner}
          onPress={() => router.push("/appstore" as any)}
        >
          <View style={styles.appStoreLeft}>
            <Ionicons name="apps" size={24} color="#FFFFFF" />
            <View style={styles.appStoreText}>
              <Text style={styles.appStoreTitle}>AppStore</Text>
              <Text style={styles.appStoreDesc}>Discover apps from developers across Africa</Text>
            </View>
          </View>
          <Ionicons name="chevron-forward" size={20} color="#FFFFFF" />
        </TouchableOpacity>

        {/* Layer Sections */}
        {LAYERS.map(renderLayerSection)}

        {/* Intelligence Layer */}
        {renderIntelligenceLayer()}

        {/* Bottom Spacer */}
        <View style={{ height: 40 }} />
      </ScrollView>
    </SafeAreaView>
  );
}

// ─── Styles ───
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#0F172A",
  },
  backgroundImage: {
    position: "absolute",
    width: width,
    height: height,
    opacity: 0.35,
  },
  overlay: {
    position: "absolute",
    width: width,
    height: height,
    backgroundColor: "rgba(15, 23, 42, 0.75)",
  },
  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "flex-start",
    paddingHorizontal: 20,
    paddingTop: Platform.OS === "ios" ? 8 : 16,
    zIndex: 10,
  },
  greeting: {
    fontSize: 14,
    color: "#94A3B8",
    fontWeight: "500",
  },
  userName: {
    fontSize: 22,
    color: "#FFFFFF",
    fontWeight: "700",
    marginTop: 2,
  },
  headerActions: {
    flexDirection: "row",
    gap: 8,
  },
  iconButton: {
    width: 40,
    height: 40,
    borderRadius: 12,
    backgroundColor: "rgba(255,255,255,0.1)",
    justifyContent: "center",
    alignItems: "center",
    position: "relative",
  },
  notifBadge: {
    position: "absolute",
    top: 8,
    right: 8,
    width: 8,
    height: 8,
    borderRadius: 4,
    backgroundColor: "#EF4444",
  },
  dateContainer: {
    alignItems: "center",
    marginTop: 16,
    marginBottom: 8,
    zIndex: 10,
  },
  dayName: {
    fontSize: 13,
    color: "#94A3B8",
    fontWeight: "600",
    letterSpacing: 2,
  },
  dayNum: {
    fontSize: 56,
    color: "#FFFFFF",
    fontWeight: "200",
    lineHeight: 64,
  },
  monthYear: {
    fontSize: 14,
    color: "#CBD5E1",
    fontWeight: "400",
  },
  content: {
    flex: 1,
    zIndex: 10,
  },
  scrollContent: {
    paddingHorizontal: 16,
    paddingTop: 8,
  },
  appStoreBanner: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    backgroundColor: "rgba(99, 102, 241, 0.25)",
    borderWidth: 1,
    borderColor: "rgba(99, 102, 241, 0.4)",
    borderRadius: 16,
    padding: 16,
    marginBottom: 20,
  },
  appStoreLeft: {
    flexDirection: "row",
    alignItems: "center",
    gap: 12,
  },
  appStoreText: {
    gap: 2,
  },
  appStoreTitle: {
    fontSize: 16,
    color: "#FFFFFF",
    fontWeight: "700",
  },
  appStoreDesc: {
    fontSize: 12,
    color: "#A5B4FC",
    fontWeight: "400",
  },
  layerSection: {
    marginBottom: 24,
  },
  layerHeader: {
    marginBottom: 12,
  },
  layerTitle: {
    fontSize: 11,
    fontWeight: "700",
    letterSpacing: 2,
    marginBottom: 4,
  },
  layerSubtitle: {
    fontSize: 20,
    color: "#FFFFFF",
    fontWeight: "700",
    marginBottom: 4,
  },
  layerDesc: {
    fontSize: 13,
    color: "#94A3B8",
    fontWeight: "400",
    lineHeight: 18,
  },
  appsGrid: {
    flexDirection: "row",
    flexWrap: "wrap",
    gap: 8,
  },
  appTile: {
    width: (width - 40) / 3,
    backgroundColor: "rgba(30, 41, 59, 0.85)",
    borderRadius: 16,
    padding: 12,
    alignItems: "flex-start",
    borderWidth: 1,
    borderColor: "rgba(255,255,255,0.06)",
  },
  iconContainer: {
    width: 44,
    height: 44,
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 8,
    position: "relative",
  },
  badge: {
    position: "absolute",
    top: -4,
    right: -4,
    backgroundColor: "#10B981",
    borderRadius: 6,
    paddingHorizontal: 5,
    paddingVertical: 1,
    borderWidth: 1,
    borderColor: "#0F172A",
  },
  badgeText: {
    fontSize: 8,
    color: "#FFFFFF",
    fontWeight: "700",
  },
  appName: {
    fontSize: 13,
    color: "#FFFFFF",
    fontWeight: "600",
    marginBottom: 2,
  },
  appDesc: {
    fontSize: 10,
    color: "#64748B",
    fontWeight: "400",
    lineHeight: 14,
  },
  // Intelligence Layer
  viewSwitcher: {
    flexDirection: "row",
    backgroundColor: "rgba(30, 41, 59, 0.8)",
    borderRadius: 12,
    padding: 4,
    marginBottom: 16,
    gap: 4,
  },
  viewButton: {
    flex: 1,
    paddingVertical: 8,
    paddingHorizontal: 4,
    borderRadius: 8,
    alignItems: "center",
  },
  viewButtonActive: {
    backgroundColor: "rgba(255,255,255,0.15)",
  },
  viewButtonText: {
    fontSize: 11,
    color: "#94A3B8",
    fontWeight: "500",
  },
  viewButtonTextActive: {
    color: "#FFFFFF",
    fontWeight: "600",
  },
  mapCard: {
    backgroundColor: "rgba(30, 41, 59, 0.85)",
    borderRadius: 16,
    padding: 16,
    marginBottom: 12,
    borderWidth: 1,
    borderColor: "rgba(255,255,255,0.06)",
  },
  mapHeader: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginBottom: 12,
  },
  mapTitle: {
    fontSize: 13,
    color: "#FFFFFF",
    fontWeight: "600",
  },
  mapDropdown: {
    flexDirection: "row",
    alignItems: "center",
    gap: 4,
    backgroundColor: "rgba(0,0,0,0.3)",
    paddingHorizontal: 10,
    paddingVertical: 4,
    borderRadius: 6,
  },
  mapDropdownText: {
    fontSize: 11,
    color: "#CBD5E1",
    fontWeight: "500",
  },
  mapBody: {
    height: 140,
    backgroundColor: "rgba(15, 23, 42, 0.6)",
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
    position: "relative",
    overflow: "hidden",
  },
  mapDots: {
    position: "absolute",
    width: "100%",
    height: "100%",
  },
  mapDot: {
    position: "absolute",
    width: 10,
    height: 10,
    borderRadius: 5,
    shadowColor: "#000",
    shadowOffset: { width: 0, height: 0 },
    shadowOpacity: 0.5,
    shadowRadius: 4,
    elevation: 4,
  },
  mapLabel: {
    fontSize: 12,
    color: "#64748B",
    fontWeight: "500",
    marginTop: 8,
  },
  intelRow: {
    flexDirection: "row",
    marginBottom: 12,
  },
  intelCard: {
    backgroundColor: "rgba(30, 41, 59, 0.85)",
    borderRadius: 16,
    padding: 12,
    borderWidth: 1,
    borderColor: "rgba(255,255,255,0.06)",
  },
  intelCardHeader: {
    flexDirection: "row",
    alignItems: "center",
    gap: 6,
    marginBottom: 10,
  },
  intelCardTitle: {
    fontSize: 12,
    color: "#FFFFFF",
    fontWeight: "600",
  },
  simLabel: {
    fontSize: 9,
    color: "#64748B",
    fontWeight: "400",
  },
  activityRow: {
    flexDirection: "row",
    alignItems: "center",
    gap: 6,
    marginBottom: 6,
  },
  activityIcon: {
    fontSize: 12,
  },
  activityText: {
    fontSize: 11,
    color: "#CBD5E1",
    fontWeight: "400",
    flex: 1,
  },
  governanceCard: {
    backgroundColor: "rgba(30, 41, 59, 0.85)",
    borderRadius: 16,
    padding: 16,
    marginBottom: 12,
    borderWidth: 1,
    borderColor: "rgba(255,255,255,0.06)",
  },
  governanceHeader: {
    flexDirection: "row",
    alignItems: "center",
    gap: 8,
    marginBottom: 4,
  },
  governanceTitle: {
    fontSize: 15,
    color: "#FFFFFF",
    fontWeight: "700",
  },
  governanceSub: {
    fontSize: 12,
    color: "#64748B",
    fontWeight: "400",
    marginBottom: 12,
  },
  alertRow: {
    flexDirection: "row",
    alignItems: "flex-start",
    gap: 6,
    marginBottom: 8,
  },
  alertIcon: {
    fontSize: 12,
    marginTop: 1,
  },
  alertText: {
    fontSize: 12,
    color: "#CBD5E1",
    fontWeight: "400",
    flex: 1,
    lineHeight: 16,
  },
  systemCard: {
    backgroundColor: "rgba(30, 41, 59, 0.85)",
    borderRadius: 16,
    padding: 16,
    borderWidth: 1,
    borderColor: "rgba(255,255,255,0.06)",
  },
  systemHeader: {
    marginBottom: 10,
  },
  systemStatus: {
    flexDirection: "row",
    alignItems: "center",
    gap: 8,
  },
  statusDot: {
    width: 8,
    height: 8,
    borderRadius: 4,
  },
  systemTitle: {
    fontSize: 13,
    color: "#FFFFFF",
    fontWeight: "600",
  },
  systemMetrics: {
    gap: 8,
  },
  metric: {
    flexDirection: "row",
    alignItems: "center",
    gap: 8,
  },
  metricText: {
    fontSize: 12,
    color: "#CBD5E1",
    fontWeight: "400",
  },
  pulseDot: {
    width: 8,
    height: 8,
    borderRadius: 4,
  },
});
