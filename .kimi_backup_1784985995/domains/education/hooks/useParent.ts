import{useState,useEffect,useCallback}from'react';import{supabase}from'@/lib/supabase';import*as svc from'../services/parentService';
export function useParentConnections(parentId:string){const[data,setD]=useState<any[]>([]);const[loading,setL]=useState(true);const[error,setE]=useState<string|null>(null);const fetch=useCallback(async()=>{setL(true);try{setD(await svc.getParentConnections(parentId));setE(null);}catch(e:any){setE(e.message);}finally{setL(false);}},[parentId]);useEffect(()=>{fetch();},[fetch]);useEffect(()=>{const ch=supabase.channel('pc_'+parentId).on('postgres_changes',{event:'*',schema:'public',table:'education_parent_connections',filter:`parent_id=eq.${parentId}`},fetch).subscribe();return()=>{supabase.removeChannel(ch);};},[parentId,fetch]);return{connections:data,loading,error,refresh:fetch,connect:svc.connectParent,update:svc.updateConnection};}
export function useStudentParents(studentId:string){const[data,setD]=useState<any[]>([]);const[loading,setL]=useState(true);const[error,setE]=useState<string|null>(null);const fetch=useCallback(async()=>{setL(true);try{setD(await svc.getStudentParents(studentId));setE(null);}catch(e:any){setE(e.message);}finally{setL(false);}},[studentId]);useEffect(()=>{fetch();},[fetch]);return{parents:data,loading,error,refresh:fetch};}
export function useParentNotifications(parentId:string){const[data,setD]=useState<any[]>([]);const[loading,setL]=useState(true);const[error,setE]=useState<string|null>(null);const fetch=useCallback(async()=>{setL(true);try{setD(await svc.getParentNotifications(parentId));setE(null);}catch(e:any){setE(e.message);}finally{setL(false);}},[parentId]);useEffect(()=>{fetch();},[fetch]);return{notifications:data,loading,error,refresh:fetch};}
