import React,{useState,useEffect}from'react';import{View,Text,TouchableOpacity,ActivityIndicator}from'react-native';import{useQRSession}from'../hooks/useIdentity';import{Ionicons}from'@expo/vector-icons';import QRCode from'react-native-qrcode-svg';
export default function QRIdentityScreen({institutionId}:{institutionId:string}){const{session,loading,error,refresh,create}=useQRSession(institutionId);const[code,setCode]=useState('');
useEffect(()=>{if(session?.session_code)setCode(session.session_code);},[session]);
const generate=async()=>{const code='QR-'+Math.random().toString(36).substring(2,10).toUpperCase();const exp=new Date();exp.setHours(exp.getHours()+24);await create({institution_id:institutionId,session_code:code,expires_at:exp.toISOString(),is_active:true});await refresh();};
if(loading)return<View style={{flex:1,justifyContent:'center',alignItems:'center'}}><ActivityIndicator size="large"color="#3B82F6"/></View>;
if(error)return<View style={{flex:1,justifyContent:'center',alignItems:'center',padding:20}}><Ionicons name="warning"size={40}color="#EF4444"/><Text style={{marginTop:12,color:'#EF4444'}}>{error}</Text><TouchableOpacity onPress={refresh}style={{marginTop:16,backgroundColor:'#3B82F6',paddingHorizontal:20,paddingVertical:10,borderRadius:20}}><Text style={{color:'#fff',fontWeight:'600'}}>Retry</Text></TouchableOpacity></View>;
return(<View style={{flex:1,backgroundColor:'#F9FAFB',alignItems:'center',paddingTop:60}}><Text style={{fontSize:22,fontWeight:'800',color:'#111827',marginBottom:8}}>QR Identity</Text><Text style={{fontSize:13,color:'#6B7280',marginBottom:30}}>Scan for attendance & access</Text>{code?<><View style={{backgroundColor:'#fff',padding:20,borderRadius:20,shadowColor:'#000',shadowOffset:{width:0,height:2},shadowOpacity:0.1,shadowRadius:8,elevation:4}}><QRCode value={code}size={200}/></View><Text style={{fontSize:16,fontWeight:'700',color:'#111827',marginTop:20}}>{code}</Text><Text style={{fontSize:12,color:'#6B7280',marginTop:4}}>Expires: {session?.expires_at?new Date(session.expires_at).toLocaleString():'N/A'}</Text><Text style={{fontSize:12,color:'#9CA3AF',marginTop:2}}>Scans: {session?.scan_count||0}</Text></>:<Text style={{fontSize:15,color:'#6B7280',marginBottom:20}}>No active QR session</Text>}<TouchableOpacity onPress={generate}style={{backgroundColor:'#3B82F6',borderRadius:14,padding:16,alignItems:'center',marginTop:30,width:200}}><Text style={{color:'#fff',fontWeight:'700',fontSize:15}}>{code?'Regenerate':'Generate QR'}</Text></TouchableOpacity></View>);}
