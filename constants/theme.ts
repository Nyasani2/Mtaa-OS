import { Colors } from './Colors';

// Re-export as uppercase to match AppStore imports
export const COLORS = Colors;

// Font family constants
export const FONTS = {
  regular: 'System',
  medium: 'System',
  bold: 'System',
  light: 'System',
  semibold: 'System',
};

// Spacing / sizing constants
export const SIZES = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
};
